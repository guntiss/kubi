# `npm version` bumps package.json only -- no commit, no tag. The release
# workflow publishes on v* tags, so the tag is made by hand, against a commit
# that already carries both the bump and its changelog entry.
git-tag-version=false
